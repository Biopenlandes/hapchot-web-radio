/* tslint:disable:no-unused-variable */

import { addProviders, async, inject } from '@angular/core/testing';
import {Hangout} from './hangout';

describe('Hangout', () => {
  it('should create an instance', () => {
    expect(new Hangout()).toBeTruthy();
  });
});
