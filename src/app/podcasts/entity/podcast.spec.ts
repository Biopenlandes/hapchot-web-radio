/* tslint:disable:no-unused-variable */

import { addProviders, async, inject } from '@angular/core/testing';
import {Podcast} from './podcast';

describe('Podcast', () => {
  it('should create an instance', () => {
    expect(new Podcast()).toBeTruthy();
  });
});
